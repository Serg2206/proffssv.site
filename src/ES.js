// Восстановленный компонент: ES
// Источник: CDSS Острый Живот 2026

function ES(e){let t=S$(e),n=t.matches[t.matches.length-1];return pt(n.route.id,`${e} can only be used on routes that contain a unique "id"`),n.route.id}