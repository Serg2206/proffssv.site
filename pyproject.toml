[tool.poetry]
name = "proffssv-site"
version = "1.0.0"
description = "Site sources for proffssv.site"
authors = ["Серж Сушков"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
mkdocs = "^1.6"
mkdocs-material = "^9.5"
pymdown-extensions = "^10.7"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
