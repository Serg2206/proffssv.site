[project]
name = "proffssv.site"
version = "1.0.0"
description = "Официальный сайт профессора Сушкова"
readme = "README.md"
requires-python = ">=3.11"

[project.optional-dependencies]
docs = [
    "mkdocs>=1.5.0",
    "mkdocs-material>=9.4.0",
]